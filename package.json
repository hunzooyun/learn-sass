{
  "scripts": {
    "serve": "live-server --port=8090 --host=localhost",
    "sass": "sass src/syntax:src/css",
    "watch": "npm run sass -- --watch",
    "update": "npm run watch -- --update",
    "remove": "rm -rf ./src/css",
    "build": "npm run sass -- --style compressed --no-source-map"
  },
  "devDependencies": {
    "live-server": "^1.2.2",
    "npm-run-all": "^4.1.5",
    "sass": "^1.80.3"
  }
}
